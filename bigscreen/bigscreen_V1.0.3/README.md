# Pad App: 移动屏应用 1.0.3版本

```
$ npm install 
```

## Pad App Preview

Pad App use gulp dev server to develop, Just run it in repo's root:

```
$ gulp default
```

OR change the host or port

```
$ gulp default --host [your ip] --port [your port]
```
Pad App will be available on `http://[your ip]:[your port]/`